# V3.8.4 Test Report

## 已完成驗證

- `node --check app.js`：PASS
- `node --check heic-worker.js`：PASS
- `node --check image-worker.js`：PASS
- HTML ID 檢查：186 個 ID，重複 0：PASS
- V3.8.4 版本／操作紀錄 UI／事件綁定靜態檢查：PASS
- `package.json` 版本 3.8.4：PASS
- GitHub Pages workflow 名稱 V3.8.4：PASS
- `.github/workflows/pages.yml` 與根目錄備用 workflow 內容一致：PASS
- 操作紀錄單元測試：PASS
  - 1,500 筆上限
  - TXT / CSV / JSON 產生
  - CSV `= + - @` 公式注入防護
- `.photojob` manifest `operation_log` 結構驗證：PASS
  - 正常紀錄可通過
  - 超過欄位長度會拒絕
- Undo / Redo 不會回捲操作紀錄：結構回歸 PASS
- Autosave／手動暫存／`.photojob` 均包含 `operation_log`：PASS
- 隱私靜態回歸：PASS
  - 照片說明、備註、照片地點的操作紀錄不引用欄位全文
- 安全靜態檢查：PASS
  - runtime JS 未發現 `eval()` / `new Function()`
  - runtime JS 未新增 `fetch` / XHR / WebSocket 上傳路徑
- 最終 ZIP 完整性與解壓後 Node / HTML / workflow 重驗：PASS

## 操作紀錄設計說明

- 操作紀錄上限為 1,500 筆，超過時保留最新紀錄。
- 操作紀錄不納入 Undo / Redo 歷史，因此復原編輯不會一併刪除稽核軌跡。
- 為避免大型專案每新增一筆紀錄就重寫全部照片 Blob，單純新增操作紀錄不會額外觸發整份 IndexedDB 寫入；紀錄會隨一般 Autosave、手動暫存、關頁保存與 `.photojob` 保存持久化。
- 本功能為工作追蹤／稽核輔助資料，可由使用者清除，不屬不可竄改的電子稽核憑證。

## 瀏覽器 E2E 限制

已嘗試使用本機 Chromium 執行互動測試，但本執行環境對 `http://127.0.0.1` 與 `file://` 導航回傳 `ERR_BLOCKED_BY_ADMINISTRATOR`。因此本次不宣稱完成真瀏覽器互動 E2E；此項為測試環境限制，而不是瀏覽器測試 PASS。

部署至 GitHub Pages 後，建議用 2～3 張測試照片實際驗證：匯入 → 修改說明 → 暫存 → 匯出 TXT/CSV/JSON → 儲存 `.photojob` → 重新開啟 `.photojob`，確認操作紀錄仍存在。
