# V3.7 Test Report

本報告只記錄本次實際重新執行的檢查，不把歷代自述報告當成驗證證據。

## 已執行

- JavaScript / MJS 語法檢查。
- 靜態資安回歸檢查。
- HTML parser 與重複 ID 檢查。
- CSS brace 結構檢查。
- GitHub Actions YAML 解析。
- ZIP 完整性與封裝後重新檢查。
- Chromium 核心互動 harness：1366×768 照片清單、照片匯入、個資遮蔽建立／重新選取／刪除、超長圖片預覽切頁、pageerror。

## Chromium 核心互動結果

- 1366×768 加入 3 張照片後照片清單約 434px，可操作。
- 個資遮蔽可建立並保存。
- 重開註記工具可選取既有遮蔽並刪除。
- 300×1400 測試長圖在智慧切頁開啟時產生 2 張預覽頁。
- 該 harness 執行期間未觀察到 `pageerror`。

## 限制

測試環境的 Chromium 管理政策會阻擋 `localhost`／`127.0.0.1`／`file://`，因此本地完整 HTTP E2E 無法在此環境宣稱通過。核心瀏覽器互動使用受控 inline harness 驗證；Repository 內仍保留 GitHub Actions 的完整 Playwright regression gate。
