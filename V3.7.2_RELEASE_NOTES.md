# V3.7.2 Release Notes

## HEIC Fast Import & Parallel Decode Edition

本版針對大量 iPhone HEIC / HEIF 匯入時間進行核心解碼流程優化。

### 解碼架構

- 低記憶體裝置：1 路 Worker
- 一般裝置：2 路 Worker
- 高記憶體／多核心裝置：3 路 Worker

Worker 會持續存在並重複處理下一張照片，不再每張照片都重新建立 Worker 與載入 HEIC decoder。

### 原生 HEIC 探測快取

若瀏覽器第一次已確認無法原生解碼 HEIC，後續照片不再重複嘗試原生解碼，直接交給隔離 Worker。

### 移除重複完整 decode

HEIC decoder 已經產生合法 JPEG 後，主程式改用 JPEG header 與尺寸安全檢查，不再把整張 JPEG 再完整解碼一次。

### 可靠性

- Worker timeout 僅重建故障 slot。
- 使用者取消匯入時，所有執行中與等待中的 HEIC 任務會一起取消。
- 匯入結果仍依使用者原始選取順序加入清單。
- 保留 V3.7.1 大型照片數量與縮圖穩定性修正。
