# V3.8 Release Notes

## Large Project Control

- 新增大型專案資源監控與容量條。
- Chromium 可提供 `performance.memory` 時，顯示 JS Heap 使用量；其他瀏覽器以專案圖片容量／安全上限作為保守指標。
- 新增最近一次匯入摘要與失敗數。
- 新增「重試失敗照片」，只重跑失敗檔案。

## Search & Batch Workflow

新增進階篩選：

- 全部照片
- 缺少說明
- 缺少地點
- HEIC / HEIF
- 有註記
- 排除輸出
- 已勾選

新增：

- 批次章節／分類
- 批次排除輸出
- 批次恢復輸出

## Export Workflow

- 單張照片可標記「保留在專案，但不要輸出」。
- 輸出範圍可選「全部可輸出照片」或「只輸出已勾選照片」。
- Word、PDF、列印預覽與長圖切頁共用同一個輸出照片集合。
- 輸出前缺漏檢查與案件封存只檢查實際輸出範圍。
- 被排除照片仍完整保存在 `.photojob`，日後可恢復輸出。

## Data Compatibility

- `.photojob` Schema 維持 2。
- 新增可選欄位 `exclude_export`。
- `meta.export_scope` 僅允許 `included` 或 `selected`。
- V3.7.x 舊專案缺少新欄位時自動以 `false / included` 處理。
