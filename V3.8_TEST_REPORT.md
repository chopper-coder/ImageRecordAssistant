# V3.8 Test Report

測試日期：2026-08-23

## 靜態檢查

- `app.js`：Node syntax PASS
- `heic-worker.js`：Node syntax PASS
- `image-worker.js`：Node syntax PASS
- `package.json`：JSON parse PASS
- GitHub Pages YAML：parse PASS
- HTML：175 個 ID，重複 0
- JS 靜態 `$('<id>')` 參照：除動態建立的 `previewImg` 外，均可在 HTML 找到

## Chromium synthetic-document regression

因執行環境管理政策會阻擋 `file://`、localhost 與任意測試網址，本次使用 Chromium `page.set_content()` 載入相同 HTML/CSS/JS 進行真 DOM / Canvas 互動回歸；未使用模擬 DOM library。

### 149 張大量照片

- 149 / 149 照片列建立：PASS
- V3.8 badge：PASS
- 選取 3 張 → 「只輸出已勾選」顯示 3 張：PASS
- 其中 1 張標記不輸出 → 輸出數立即變 2 張：PASS
- 「排除輸出」篩選只顯示 1 張：PASS
- 「缺少說明」篩選顯示 149 張：PASS
- 批次章節套用至 3 張：PASS
- 只輸出 2 張、每頁 2 張 → 列印版面預覽 1 頁：PASS
- Browser page error：0

### 匯入失敗與重試

以 1 張合法 PNG + 1 個偽造 JPG 測試：

- 成功 1、略過 1 摘要：PASS
- 失敗數顯示 1：PASS
- 「重試失敗照片」按鈕啟用：PASS
- 重試後仍失敗者保留於失敗清單：PASS
- Browser page error：0

### Undo / Redo

- 單張「排除輸出」→ Undo 恢復：PASS
- Redo 再次排除：PASS
- Browser page error：0

## 未宣稱項目

本輪沒有重新以真實 100+ 張 iPhone HEIC 做效能基準；V3.8 未變更 V3.7.2 已穩定的 HEIC Worker 核心，只在其上增加大型專案控制與輸出流程。
